image: mhart/alpine-node:8

before_script:
  - alias gulp='$(yarn bin)/gulp'

babel:
  stage: build
  tags: ['docker']
  script:
    - yarn install
    - gulp build
  artifacts:
    paths:
      - node_modules/
      - lib/
    expire_in: 1 week

formatting:
  stage: test
  tags: ['docker']
  script: gulp test-format

holidays:
  stage: test
  tags: ['docker']
  script: gulp test-holidays

