image: mhart/alpine-node:8

babel:
  stage: build
  tags: ['docker']
  script:
    - yarn install
    - gulp build
  artifacts:
    paths:
      - node_modules/
      - lib/
    expire_in: 1 week

formatting:
  stage: test
  tags: ['docker']
  script: gulp test-format

holidays:
  stage: test
  tags: ['docker']
  script: gulp test-holidays

